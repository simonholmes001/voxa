# Changesets

Add a changeset for releasable changes under `backend/`, `ios/`, `functions/`, `infrastructure/`, `tests/`, `scripts/`, or root package metadata.

Use this format:

```md
---
"voxa": patch
---

Short description of the user-facing or operational change.
```

Use `patch`, `minor`, or `major` according to the release impact.
