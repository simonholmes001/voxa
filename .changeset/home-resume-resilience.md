---
"voxa": patch
---

Make the iOS Home resume flow fall back to the locally saved learner profile when the backend is temporarily unavailable, while preserving normal loading when no resume service is configured.
