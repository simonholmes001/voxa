# Voxa Privacy and Security Assurance Baseline

Status: draft assurance baseline for MVP and App Store readiness.
Owner: Codex.
Related issue: #85.
Last reviewed: 2026-09-15.

This document records the current evidence-backed privacy, data handling, and
security posture for Voxa. It is not legal advice. It is the engineering
control baseline used to decide what must be fixed before App Store submission
and broader testing.

## Scope

Voxa is an iPhone/iPad language tutor backed by Azure Functions, Azure Storage,
OpenAI APIs, Apple Sign in, and TestFlight/App Store distribution.

In scope:

- iOS app storage, permissions, privacy manifest, and App Store disclosures.
- Backend authentication, learner state, AI calls, logs, and telemetry.
- Azure infrastructure controls in Bicep.
- GDPR-style data inventory, retention, user rights, and processor evidence.

Out of scope for this baseline:

- Legal approval of the final privacy policy.
- Production Data Protection Impact Assessment sign-off.
- Paid subscription entitlement policy.

## Official Requirements Checked

Apple requirements are tracked from official Apple Developer sources:

- App Review Guidelines, especially 5.1 Privacy.
- User Privacy and Data Use.
- App Store Connect App Privacy details.
- Privacy manifest files.
- Required reason API declarations.
- Account deletion requirement for apps with account creation.

GDPR posture is assessed against the core principles of lawfulness,
fairness/transparency, purpose limitation, data minimisation, accuracy, storage
limitation, integrity/confidentiality, and accountability.

## Data Inventory

| Data | Source | Stored on Device | Stored in Azure | Sent to OpenAI | Purpose | Current Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Apple identity token and authorization code | Sign in with Apple | No durable storage intended | No durable storage intended | No | Account sign-in | `VoxaBackendAuthenticationService`, `SignInWithAppleEndpoint` tests assert proofs are not echoed | partial |
| Voxa access and refresh tokens | Backend auth response | Keychain, this-device-only | Refresh sessions in Azure Table storage | No | Authenticated app session | `KeychainSessionStore`, `TableRefreshSessionStore`, refresh-token hash row key tests | partial |
| Tenant ID and user ID | Backend session principal | In auth session/profile scope | Learner state partition and refresh-session payload | No | Tenant/user isolation | `HmacAppSessionTokenIssuer`, `TableLearnerStateRepository` | partial |
| Learner profile | Onboarding/settings | Local draft and UI state | LearnerState table | Yes, during course generation/reassessment where applicable | Personalised course and tutoring | `OnboardingService`, `TableLearnerStateRepository`, course author services | partial |
| Language profiles and active language | Backend/iOS settings | Cached in UI state | LearnerState table and active-language marker | No direct OpenAI call | Cross-device continuity | `LanguageProfileService`, `TableLearnerStateRepository` | partial |
| Course and lesson plan | OpenAI/course author and backend state | Display/cache state | LearnerState table | Yes, generated by OpenAI | Personalised learning plan | `OpenAiCourseAuthorService`, learner plan/course endpoints | partial |
| Practice questions and translation text | User text/voice input | Transient view-model state | Not intended for durable storage | Yes | Ask/translate language tool | `PracticeLanguageToolEndpoint`, `OpenAiPracticeLanguageToolService` | partial |
| Translation images | Camera/photo picker | Transient selected image data | Not intended for durable storage | Yes, normalized base64 data URL | Image translation | `ImageTranslationPayloadPreparer`, image payload validation tests | partial |
| Realtime audio and transcripts | Microphone/OpenAI Realtime | Transient transport transcript | Debrief evidence can persist derived content | Yes | Spoken tutoring and post-session debrief | `WebSocketRealtimeTransport`, `RealtimeDebriefEndpoint`, `LearnerEvidenceService` | partial |
| Debrief summaries, mistakes, useful phrases | OpenAI debrief | Display state | Learner state evidence | Yes, generated by OpenAI | Progress and next-practice recommendations | `OpenAiDebriefService`, `LearnerEvidenceService` | partial |
| Realtime audit/rate-limit rows | Backend session issuance | No | Azure Table storage | No | Abuse control and cost guardrails | `TableRealtimeSessionAuditLog`, `TableRealtimeSessionRateLimiter` | partial |
| Diagnostic logs and request telemetry | Backend/iOS logging | OS logging on device | Application Insights / Log Analytics | No | Operations and debugging | `host.json`, Bicep diagnostic settings, logger usage | gap |

Status definitions:

- `meets`: control is implemented and evidenced.
- `partial`: design exists but needs documentation, tests, retention, or runtime evidence.
- `gap`: missing or materially incomplete.

## Current Security Controls

Verified in repo:

- iOS tokens use Keychain generic-password storage with
  `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`.
- Permanent OpenAI API keys are backend-only and loaded through Key Vault
  references in Azure deployment.
- Realtime clients receive short-lived OpenAI client credentials rather than the
  permanent API key.
- Backend access tokens are HMAC-signed, expire, and are validated before
  authenticated endpoints access learner state.
- Refresh tokens are random and stored by hash-derived row key, not raw token.
- Learner state uses explicit tenant/user scope and optimistic versions.
- Azure Storage disables blob public access and shared-key access, requires
  HTTPS, and defaults to OAuth authentication.
- Key Vault has RBAC authorization and soft delete enabled.
- Function App uses managed identity and Key Vault references.
- Log Analytics and Application Insights retention are configured to 30 days.

Not yet fully evidenced:

- Runtime Azure configuration after deployment matches Bicep defaults.
- All OpenAI paths avoid logging raw upstream bodies or generated learner
  content.
- Account deletion removes every relevant learner, refresh-session, audit,
  evidence, and diagnostic record.
- App Store Connect App Privacy answers match actual app behavior.
- Processor/subprocessor and international-transfer records exist.

## App Store Privacy Baseline

The iOS privacy manifest declares:

- No tracking and no tracking domains.
- User ID, other user content, audio data, photos/videos, product interaction,
  and diagnostic data as collected data categories.
- Data is marked linked to the user and not used for tracking.
- UserDefaults access with reason `CA92.1`, because the app writes
  app-specific onboarding draft and tutor preference values.

This is intentionally conservative for the MVP. Before App Store submission,
generate Xcode's privacy report and reconcile it with App Store Connect App
Privacy answers.

Required App Store work before submission:

- Publish a privacy policy URL and expose it in-app.
- Add an in-app account deletion initiation path.
- Confirm the App Privacy answers cover Apple Sign in identifiers, learner
  profile, generated course content, voice/audio, transcripts, translation
  images, diagnostics, and third-party processing.
- Confirm bundled third-party SDK privacy manifests are present and accurate.
- Confirm permission copy for microphone, speech recognition, camera/photo
  library, and notifications is specific to Voxa's feature use.

## GDPR Engineering Mapping

| Principle / Right | Current Position | Evidence | Gap |
| --- | --- | --- | --- |
| Transparency | No user-facing privacy policy in repo | issue #85 | Add policy URL and in-app link |
| Purpose limitation | Main purposes are learning, auth, operations, cost control | docs/api-contracts.md, backend services | Formal purpose table needed |
| Data minimisation | Image payloads are normalized and capped; practice text capped | practice endpoint tests | Retention and transcript minimisation not complete |
| Storage limitation | Logs configured for 30 days | Bicep Log Analytics/App Insights | Learner/evidence/audit retention not defined |
| Security | Strong baseline for tokens, Key Vault, managed identity, storage | Bicep, Keychain, token tests | Runtime evidence and full logging pass needed |
| Access/export | No user-facing export path | none found | Build export endpoint/UI or document MVP deferral |
| Erasure | Dev reset and per-language deletion exist | API/iOS code | Account deletion path missing |
| Correction | Per-language settings can be edited | iOS settings/backend onboarding update | Full account/profile correction flow needs policy |
| Portability | No export format defined | none found | Define JSON export schema |
| Processor accountability | OpenAI/Azure/Apple usage visible | code and IaC | DPA/subprocessor records missing |

## Logging and Telemetry Rules

Backend and iOS logs must not contain:

- Auth tokens, refresh tokens, Apple identity tokens, or authorization codes.
- Permanent OpenAI API keys or short-lived Realtime client secrets.
- Raw OpenAI request/response bodies.
- Learner free text, translated text, transcripts, uploaded image text, or
  generated sensitive content.
- Full tenant/user identifiers unless a specific operational reason exists and
  the value is hashed or otherwise minimized.

Allowed by default:

- Status codes, endpoint names, byte counts, correlation IDs, error classes,
  model route identifiers, and bounded counts/durations.

Secure debug logging, if ever added, must be explicitly gated by environment,
disabled in production, time-limited, and redacted.

## Follow-Up Work Required

These are implementation issues, not documentation nice-to-haves:

1. Add in-app privacy policy link and account deletion initiation flow.
2. Add account deletion backend endpoint that deletes or anonymizes learner
   state, refresh sessions, active-language markers, debrief evidence, and
   associated operational records where legally permissible.
3. Add learner data export endpoint and in-app export request surface.
4. Add retention controls for learner state, debrief evidence, realtime audit,
   rate-limit rows, and generated course content.
5. Redact older OpenAI service logging so no raw upstream body/model output is
   logged outside the already-hardened practice tool path.
6. Add App Store Connect privacy-answer checklist and reviewer notes before
   first external submission.
7. Validate deployed Azure resources against Bicep security expectations.

## Release Gate

Before external TestFlight expansion or App Store submission:

- Privacy policy URL exists and matches this inventory.
- App Store Connect App Privacy answers are completed from this inventory.
- `PrivacyInfo.xcprivacy` is reviewed against Xcode's generated privacy report.
- Account deletion is implemented or App Store submission is blocked.
- Raw learner-content logging is removed or proven absent across AI paths.
- Retention/export/deletion follow-up issues are either complete or accepted as
  a documented MVP residual risk by the owner.
